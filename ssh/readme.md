# SSH

## Connect SSH
```bash
ssh user@host
```

## Copy folders
```bash
scp -r user@host:/path/to/file  /path/to/new/file
```

### Copy file
```bash
scp user@host:/path/to/file  /path/to/new/file
```
